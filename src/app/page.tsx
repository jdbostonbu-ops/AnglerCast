// Placeholder home page so the project runs. The real landing page is feature #9 in the build order.
const HomePage = () => {
  return (
    <main>
      <h1>AnglerCast</h1>
      <p>Starter scaffold. Build features in the order set out in AGENTS.md, test-first.</p>
    </main>
  );
};

export default HomePage;
