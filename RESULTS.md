# AnglerCast TDD Results

## Add results of RED/GREEN tests below

| Test | RED result | GREEN result | Notes |
|------|------------|--------------|-------|
| 1.1 | RED: `src/lib/emailVerification.test.ts` imports `createEmailVerificationCode` from `@/lib/emailVerification`, but the implementation did not export it yet, so the test fails for the expected missing implementation. | GREEN: `createEmailVerificationCode` was added in `src/lib/emailVerification.ts`, returning the raw `code`, hashed `verificationCodeHash`, and future `expiresAt` timestamp expected by the test. | Checks that the stored verification code is hashed, bcrypt verifies the raw emailed code against the stored hash, and the expiry timestamp is in the future. |
| 1.2 | RED: `src/lib/emailVerification.test.ts` imports `verifyEmailVerificationCode` from `@/lib/emailVerification`, but the implementation did not export it yet, so the test fails for the expected missing verify function. | GREEN: `verifyEmailVerificationCode` was added in `src/lib/emailVerification.ts`, accepting `enteredCode`, `verificationCodeHash`, and `expiresAt`, then returning `true` for a correct unexpired code. | Checks that a correct entered code verifies true when it matches the stored hash and the expiry timestamp is still in the future. |
| 1.3 | RED: `src/lib/emailVerification.test.ts` expects an expired correct code to resolve to `{ isVerified: false, reason: 'expired' }`, but the current implementation returned a bare `false` before the expiry handling was added. | GREEN: `verifyEmailVerificationCode` now returns `{ isVerified: false, reason: 'expired' }` for expired codes in `src/lib/emailVerification.ts`. | Checks that an expired code is rejected with a clear expired reason. |
| 1.4 | RED: `src/lib/emailVerification.test.ts` expects a wrong code to resolve to `{ isVerified: false, reason: 'mismatch' }`, but the implementation did not return a mismatch reason yet. | GREEN: `verifyEmailVerificationCode` now returns `{ isVerified: false, reason: 'mismatch' }` for wrong codes in `src/lib/emailVerification.ts`. | Checks that a wrong entered code is rejected with a clear mismatch reason. |
| 1.5 | RED: `src/lib/emailVerification.test.ts` imports `verifyEmailVerificationCodeAndActivateAccount` from `@/lib/emailVerification`, but the implementation did not export it yet, so the test fails for the expected missing account activation flow. | GREEN: `verifyEmailVerificationCodeAndActivateAccount` was added in `src/lib/emailVerification.ts`; it finds the user by email, verifies the entered code, sets `isVerified` to `true`, and clears `verificationCodeHash` and `verificationCodeExpiresAt`. | Checks that a new account starts inactive and flips active only after successful email verification, with Prisma mocked. |
| 1.6 | RED: `src/lib/emailVerification.test.ts` expects `verifyEmailVerificationCodeAndActivateAccount` to reject an already-verified account with `{ isVerified: false, reason: 'already_verified' }`, but the implementation did not have that guard yet. | GREEN: `verifyEmailVerificationCodeAndActivateAccount` now checks `account.isVerified` before verifying the code or updating the user, returning `{ isVerified: false, reason: 'already_verified' }`. | Checks that an already-verified account cannot be verified again and `prisma.user.update` is not called. |
| Verify messages | RED: `src/lib/verifyMessages.test.ts` imports `messageForVerifyReason` from `@/lib/verifyMessages`, but the implementation file was empty, so the test fails for the expected missing verify-message mapper. | GREEN: `messageForVerifyReason` was added in `src/lib/verifyMessages.ts`; it maps `expired`, `mismatch`, and `already_verified` to the tested user-facing strings and uses one generic fallback for unknown or undefined reasons. | Checks that verification failures produce clear user-facing copy without leaking implementation reason codes. |
| 2.1 | RED: `src/lib/login.test.ts` imports `checkLoginCredentials` from `@/lib/login`, but the implementation module did not exist yet, so the test fails for the expected missing credential-check function. | GREEN: `checkLoginCredentials` was added in `src/lib/login.ts`; it finds the user by email, requires `isVerified`, compares the entered password with `bcrypt.compare`, and returns `{ isAuthenticated: true, user: { id, email } }` for a verified user with the correct password. | Checks that a verified user can log in with the correct email and password, with Prisma and bcrypt mocked. |
| 2.2 | RED: `src/lib/login.test.ts` expects a verified user with a wrong password to resolve to `{ isAuthenticated: false, reason: 'wrong_password' }`, but the implementation returned the generic `invalid_credentials` reason. | GREEN: `checkLoginCredentials` now returns `{ isAuthenticated: false, reason: 'wrong_password' }` when `bcrypt.compare` returns `false`. | Checks that a wrong password is rejected with a clear wrong-password reason, with Prisma and bcrypt mocked. |
| 2.3 | RED: `src/lib/login.test.ts` expects an unverified user to resolve to `{ isAuthenticated: false, reason: 'email_not_verified' }`, but the implementation returned the older `unverified` reason. | GREEN: `checkLoginCredentials` now returns `{ isAuthenticated: false, reason: 'email_not_verified' }` before calling `bcrypt.compare` when the user is not verified. | Checks that an unverified user cannot log in even with the correct password, with Prisma and bcrypt mocked. |
| Session cookie | RED: `src/lib/session.test.ts` imports `createSession` and `getSessionUserId` from `@/lib/session`, but the implementation module did not exist yet, so the test fails for the expected missing session-cookie helpers. | GREEN: `createSession` and `getSessionUserId` were added in `src/lib/session.ts`; they use Next.js `cookies()` to set and read the `anglercast_session` cookie, returning the stored user id or `null` when absent. | Checks session behavior with `next/headers` mocked, without a real request. |
| Clear session | RED: `src/lib/clearSession.test.ts` imports `clearSession` from `@/lib/session` and expects it to delete the `anglercast_session` cookie, but the session module did not export that helper yet. | GREEN: `clearSession` was added in `src/lib/session.ts`; it awaits Next.js `cookies()` and calls `delete` with the shared `anglercast_session` cookie name. | Checks logout/session clearing behavior through the mocked `next/headers` cookie seam. |
| 2.4 | RED: `src/components/PasswordField.test.tsx` imports `PasswordField` from `@/components/PasswordField`, but the component did not exist yet, so the test fails for the expected missing password-field implementation. | GREEN: `PasswordField` was added in `src/components/PasswordField.tsx`; it renders a labeled password input and a `Show password` button that changes the input type from `password` to `text`. | Checks that the password field starts hidden and becomes visible after clicking the show/hide toggle. |
| 2.4 button label | RED: Lines 24-35 in `src/components/PasswordField.test.tsx` expected the toggle button label to change from `Show password` to `Hide password` after clicking, but the implementation always rendered `Show password`. | GREEN: `PasswordField` now renders the toggle button label from the visibility state: `Show password` while hidden and `Hide password` while visible. | Checks that the show/hide toggle communicates the current action with the correct accessible button label. |
| 3.1 | RED: `src/lib/savedSpot.test.ts` imports `createSavedSpot` and `listSavedSpotsForUser` from `@/lib/savedSpot`, but the implementation module did not exist yet, so the test fails for the expected missing SavedSpot data access functions. | GREEN: `createSavedSpot` and `listSavedSpotsForUser` were added in `src/lib/savedSpot.ts`; they pass full-precision latitude and longitude values directly through Prisma without rounding or formatting. | Checks that creating then reading a SavedSpot returns the same full-precision coordinates, with Prisma mocked. |
| 3.2 | RED: `src/lib/savedSpot.test.ts` mocks two users' SavedSpots and expects `listSavedSpotsForUser({ userId })` to return only the requested user's spot, but the implementation returned the mocked mixed result without an ownership guard. | GREEN: `listSavedSpotsForUser` now keeps the Prisma query scoped by `where: { userId }` and filters returned rows to the requested `userId` before returning them. | Checks that a user only sees their own SavedSpots, not another user's, with Prisma mocked. |
| 3.3 | RED: `src/lib/savedSpot.test.ts` imports `updateSavedSpot` from `@/lib/savedSpot`, but the implementation did not export it yet, so the test fails for the expected missing update logic. | GREEN: `updateSavedSpot` was added in `src/lib/savedSpot.ts`; it scopes the update by `id` and `userId` and passes full-precision coordinates directly through Prisma without rounding or formatting. | Checks that updating a SavedSpot persists changes and keeps coordinates at full precision, with Prisma mocked. |
| 4.1 | RED: `src/components/DeleteConfirmationDialog.test.tsx` imports `DeleteConfirmationDialog` from `@/components/DeleteConfirmationDialog`, but the component did not exist yet, so the test fails for the expected missing delete confirmation dialog. | GREEN: `DeleteConfirmationDialog` was added in `src/components/DeleteConfirmationDialog.tsx`; clicking `Delete` opens a dialog, and clicking `Confirm` calls `onConfirmDelete` exactly once. | Checks that confirming the dialog deletes the spot only after explicit confirmation. |
| 4.2 | RED: `src/components/DeleteConfirmationDialog.test.tsx` expects a `Cancel` button to close the dialog without calling `onConfirmDelete`, but the component did not render or handle cancel yet. | GREEN: `DeleteConfirmationDialog` now renders a `Cancel` button that closes the dialog and leaves `onConfirmDelete` untouched. | Checks that canceling the delete confirmation leaves the spot untouched and closes the dialog. |
| Confirm dialog | RED: `src/components/ConfirmDialog.test.tsx` imports `ConfirmDialog` from `@/components/ConfirmDialog`, but the component did not exist yet, so the test fails for the expected missing reusable confirmation dialog. | GREEN: `ConfirmDialog` was added in `src/components/ConfirmDialog.tsx`; it is a client component, renders `null` when closed, renders the message plus default `Confirm` and `Cancel` buttons when open, and calls only the clicked handler. | Checks that the reusable dialog does not call handlers during render and keeps confirm/cancel actions separate. |
| 5.1 | RED: `src/lib/userSpots.test.ts` imports `createUserWithSavedSpot` from `@/lib/userSpots`, but the implementation module did not exist yet, so the test fails for the expected missing User-to-SavedSpot relationship helper. | GREEN: `createUserWithSavedSpot` was added in `src/lib/userSpots.ts`; it creates the user, then creates the SavedSpot with `userId` set to the created user's id while preserving full-precision coordinates. | Checks that a SavedSpot is linked to the User who created it, with Prisma mocked. |
| 6.1 | RED: `src/lib/coordinates.test.ts` imports `parseCoordinate` from `@/lib/coordinates`, but the implementation module did not exist yet, so the test fails for the expected missing coordinate parser. | GREEN: `parseCoordinate` was added in `src/lib/coordinates.ts`; it parses the input with `Number(value)` and returns `{ isValid: true, coordinate }` without rounding, truncating, or formatting the coordinate. | Checks that a full-precision coordinate survives input unchanged. |
| 6.2 | RED: `src/lib/coordinates.test.ts` expects `parseCoordinate` to reject out-of-range and non-numeric input with clear reasons, but there was no validation implementation yet. | GREEN: `parseCoordinate` now returns `{ isValid: false, reason: 'out_of_range' }` for values outside the provided bounds and `{ isValid: false, reason: 'not_numeric' }` for non-numeric input. | Checks that invalid coordinate input is rejected instead of silently becoming a wrong coordinate. |
| 6.3 | RED: `src/components/SightingRateSearch.test.tsx` expects a longitude entered with the Unicode typographic minus sign `−72.0739658` to call `onSearch` with numeric longitude `-72.0739658`, but the parser treated it as invalid. | GREEN: `parseCoordinate` now normalizes the Unicode typographic minus sign to a plain ASCII hyphen before parsing, so both latitude and longitude accept negative coordinates entered with either minus character while preserving full precision. | Checks that copied or pasted negative coordinates using `−` are accepted instead of rejected. |
| 7.1 | RED: `src/components/NavBar.test.tsx` imports `NavBar` from `@/components/NavBar`, but the implementation component did not exist yet, so the test fails for the expected missing nav bar. | GREEN: `NavBar` was added in `src/components/NavBar.tsx`; it renders `Freshwater` and `Saltwater` links with `href` values `/freshwater` and `/saltwater`. | Checks that the nav bar exposes the Freshwater and Saltwater routes. |
| 7.2 | RED: `src/components/NavBar.test.tsx` expects `About` and `Contact` links with `href` values `/about` and `/contact`, but there was no NavBar implementation to render them yet. | GREEN: `NavBar` now renders `About` and `Contact` links with the expected `/about` and `/contact` routes. | Checks that the nav bar exposes the About and Contact routes. |
| NavBar logout | RED: `src/components/NavBar.test.tsx` expects `NavBar` to accept `isLoggedIn` and `onLogout` props, render a `Log out` button only when logged in, and call `onLogout` when clicked, but the component only rendered the static links. | GREEN: `NavBar` is now a client component with optional `isLoggedIn` and `onLogout` props; it keeps the existing nav-link, water-link, and `hideWaterLinks` logic unchanged while rendering the `Log out` button inside `site-nav__links` only for logged-in users. | Checks logout affordance visibility and click behavior without changing the existing five-link navigation. |
| 8.1 | RED: `src/components/HomeButtons.test.tsx` imports `HomeButtons` from `@/components/HomeButtons`, but the implementation component did not exist yet, so the test fails for the expected missing post-login home buttons. | GREEN: `HomeButtons` was added in `src/components/HomeButtons.tsx`; it renders `Freshwater` and `Saltwater` links with `href` values `/freshwater` and `/saltwater`. | Checks that the post-login home view gives users direct routes to Freshwater and Saltwater. |
| 9.1 | RED: `src/app/page.test.tsx` expected the landing page to render a heading containing `AnglerCast` and include the `NavBar`, but `src/app/page.tsx` only rendered the heading and did not include the main navigation. | GREEN: `src/app/page.tsx` now imports and renders `NavBar` above the landing page content while keeping the `AnglerCast` heading. | Checks that the landing page includes the app heading and shared navigation. |
| 10.1 | RED: `src/lib/recommendation.test.ts` imports `createRecommendation` from `@/lib/recommendation`, but the implementation module did not exist yet, so the test fails for the expected missing recommendation selector. | GREEN: `createRecommendation` was added in `src/lib/recommendation.ts`; it selects the top species by record count, returns a real full-precision coordinate from that species' records, and calculates the peak month from the species' occurrence dates without involving AI. | Checks that recommendations are computed from real occurrence records rather than invented or AI-selected data. |
| 10.2 | RED: `src/lib/recommendation.test.ts` expected the recommendation confidence flag to use three sample-size tiers, including `confidence: 'moderate'` at `sampleSize: 10` and `confidence: 'high'` at `sampleSize: 30`, but the implementation still treated 10 or more records as `high`. | GREEN: `createRecommendation` now derives confidence from `sampleSize` using the tested thresholds: `low` under 10, `moderate` from 10 to 29, and `high` at 30 or more. | Checks that recommendations disclose sample size with the same honest confidence tiers used by sighting-rate results. |
| 10.3 | RED: `src/lib/recommendation.test.ts` expected sparse non-empty data to still return a valid recommendation with `sampleSize: 1` and `confidence: 'low'`, but the implementation did not consistently return the low-confidence metadata. | GREEN: `createRecommendation` now always returns a recommendation for non-empty input, including `sampleSize` and `confidence: 'low'` for counts below 10. | Checks that sparse data still produces an honest, non-empty recommendation. |
| 13.1 | RED: `src/lib/markerStyle.test.ts` imports `getMarkerStyle` from `@/lib/markerStyle`, but the implementation module did not exist yet, so the test fails for the expected missing species marker-style lookup. | GREEN: `getMarkerStyle` was added in `src/lib/markerStyle.ts`; it returns distinct known-species marker styles and a defined default style for unknown species. | Checks that map marker styles are reusable, species-specific, and never undefined for unknown species. |
| 14.1 | RED: `src/lib/distance.test.ts` imports `computeDistance` from `@/lib/distance`, but the implementation module did not exist yet, so the test fails for the expected missing distance calculation. | GREEN: `computeDistance` was added in `src/lib/distance.ts`; it uses the haversine formula with the full-precision origin and destination coordinates and returns nautical miles. | Checks that travel distance is computed from real coordinates without truncation. |
| 14.2 | RED: `src/lib/eta.test.ts` imports `checkEtaIsReasonable` from `@/lib/eta`, but the implementation module did not exist yet, so the test fails for the expected missing ETA sanity-check seam. | GREEN: `checkEtaIsReasonable` was added in `src/lib/eta.ts`; for the plausible 20 nautical miles at 10 knots with a 2-hour ETA case, it returns `{ isReasonable: true }`. | Checks that a plausible ETA can pass the sanity-check seam. |
| 14.3 | RED: `src/lib/eta.test.ts` expects zero and negative ETA values to return `{ isReasonable: false, reason: 'non_positive_eta' }`, but the current implementation still returns `{ isReasonable: true }`. | GREEN: Not recorded yet; implementation for RED 14.3 has not been written. | Checks that non-positive ETA values are rejected with a clear reason. |
| 15.1 | RED: `src/lib/sightingRate.test.ts` imports `computeSightingRate` from `@/lib/sightingRate`, but the implementation module did not exist yet, so the test fails for the expected missing sighting-rate function. | GREEN: `computeSightingRate` was added in `src/lib/sightingRate.ts`; it counts records whose UTC event-date month matches `selectedMonth`, returns `matchingMonthCount`, `totalCount`, and computes `rate` as `matchingMonthCount / totalCount`. | Checks that the historical sighting rate is computed from real occurrence-record dates as matching-month records divided by total records. |
| 15.2 | RED: `src/lib/sightingRate.test.ts` expects an empty record set to return `{ rate: 0, matchingMonthCount: 0, totalCount: 0 }`, but the implementation returned `NaN` for `0 / 0`. | GREEN: `computeSightingRate` now returns `rate: 0` when `totalCount` is `0`, while keeping both counts at `0`. | Checks that empty search data never divides by zero or reports a fabricated rate. |
| 15.3 | RED: `src/lib/sightingRate.test.ts` expects `computeSightingRate` to include confidence by sample size (`low` under 10, `moderate` from 10 to 29, and `high` at 30 or more), but the implementation did not return confidence yet. | GREEN: `computeSightingRate` now derives confidence from `totalCount` using the tested thresholds while preserving the existing rate, matching-month count, and total-count behavior. | Checks that sighting-rate results disclose sample size through `totalCount` and an honest confidence flag. |
| 15.4 | RED: `src/lib/occurrenceRecords.test.ts` imports `prepareOccurrenceRecordsForMap` from `@/lib/occurrenceRecords`, but the implementation module did not exist yet, so the test fails for the expected missing map-record preparation function. | GREEN: `prepareOccurrenceRecordsForMap` was added in `src/lib/occurrenceRecords.ts`; it filters out records whose latitude and longitude are both exactly `0` while preserving every other record and full-precision coordinate. | Checks that occurrence records at Null Island are excluded so no map marker is placed at `0,0`. |
| 16.1 | RED: `src/lib/occurrenceFetch.test.ts` imports `fetchOccurrenceRecords` from `@/lib/occurrenceFetch`, but the implementation module did not exist yet, so the test fails for the expected missing occurrence-fetch parser. | GREEN: `fetchOccurrenceRecords` was added in `src/lib/occurrenceFetch.ts`; it calls mocked `fetch`, parses the GBIF response `results` array, and returns records shaped with `scientificName`, `decimalLatitude`, `decimalLongitude`, and `eventDate`. | Checks that the occurrence-fetch seam parses the captured real GBIF fixture shape without hitting the network. |
| 16.1 missing results | RED: `src/lib/occurrenceFetch.test.ts` mocks a GBIF JSON response with no `results` property and expects `fetchOccurrenceRecords` to return `[]`, but the implementation called `.map` on `undefined`. | GREEN: `fetchOccurrenceRecords` now treats `gbifResponse.results` as optional and defaults to an empty array before mapping. | Checks that a malformed or empty GBIF response shape does not crash and still avoids inventing records. |
| OBIS occurrence fetch | RED: `src/lib/obisFetch.test.ts` imports `fetchObisOccurrences` from `@/lib/obisFetch`, but the implementation module did not exist yet, so the test fails for the expected missing OBIS fetch seam. | GREEN: `fetchObisOccurrences` was added in `src/lib/obisFetch.ts`; it calls `https://api.obis.org/v3/occurrence` with the `scientificname` query parameter, reads optional top-level `results`, maps OBIS records to the shared occurrence shape, and returns `[]` when results are missing. | Checks that the OBIS seam parses mocked real OBIS-shaped records without hitting the network. |
| Occurrence fetch GBIF plus OBIS | RED: `src/lib/occurrenceFetch.test.ts` expects `fetchOccurrenceRecords` to return both a mocked GBIF record and a mocked OBIS record, but the implementation only returned GBIF records. | GREEN: `fetchOccurrenceRecords` now keeps the existing GBIF query and mapping, calls `fetchObisOccurrences` with the same species and coordinates, and returns GBIF records followed by OBIS records. | Checks that OBIS augments GBIF occurrence data instead of replacing it. |
| Occurrence fetch dedupe | RED: `src/lib/occurrenceFetch.test.ts` expects overlapping GBIF and OBIS records with identical scientific name, latitude, longitude, and event date to appear only once, but the merged result returned the shared record twice. | GREEN: `fetchOccurrenceRecords` now deduplicates the concatenated GBIF-first, OBIS-second records by `scientificName`, `decimalLatitude`, `decimalLongitude`, and `eventDate`, preserving the first occurrence. | Checks that sample sizes and map records are not inflated by duplicate cross-source records. |
| 16.2 | RED: `src/app/api/auth/signup/route.test.ts` imports `POST` from `src/app/api/auth/signup/route.ts`, but the signup route handler did not exist yet, so the test fails for the expected missing route implementation. | GREEN: `POST` was added in `src/app/api/auth/signup/route.ts`; it hashes the password, creates an email verification code, creates a Prisma User with `isVerified: false`, stores the verification hash and expiry, sends the verification email, and returns the inactive user summary with status `201`. | Checks that signup wires the tested email-verification logic, Prisma user creation, and mocked email send with the expected request and response shape. |
| 16.3 | RED: `src/app/api/auth/verify/route.test.ts` imports `POST` from `src/app/api/auth/verify/route.ts`, but the verify route handler did not exist yet, so the test fails for the expected missing route implementation. | GREEN: `POST` was added in `src/app/api/auth/verify/route.ts`; it calls `verifyEmailVerificationCodeAndActivateAccount` with the submitted email and code, then returns the verified user summary with status `200`. | Checks that the verify route wires the tested email-verification activation logic without touching the real database. |
| 16.4 | RED: `src/app/api/auth/login/route.test.ts` imports `POST` from `src/app/api/auth/login/route.ts`, but the login route handler did not exist yet, so the test fails for the expected missing route implementation. | GREEN: `POST` was added in `src/app/api/auth/login/route.ts`; it calls `checkLoginCredentials`, returns the logged-in user for success, returns status `401` for wrong password, and returns status `403` for unverified email. | Checks that the login route delegates credential checking to the tested helper and rejects wrong or unverified credentials. |
| 16.4 session creation | RED: `src/app/api/auth/login/route.test.ts` mocks `createSession` from `@/lib/session` and expects successful login to call it with the authenticated user's id, but the route returned success without creating a session. | GREEN: `src/app/api/auth/login/route.ts` now imports `createSession` and calls `await createSession(result.user.id)` after credentials authenticate and before returning the success response. | Keeps wrong-password and unverified-user behavior unchanged while ensuring a verified successful login creates a session. |
| 16.4 current user | RED: `src/app/api/auth/me/route.test.ts` imports `GET` from `src/app/api/auth/me/route.ts` and mocks `getSessionUserId`, but the route file was empty, so the test fails for the expected missing current-user route. | GREEN: `GET` was added in `src/app/api/auth/me/route.ts`; it calls only `getSessionUserId`, returns `{ userId }` with status `200` when a session exists, and returns `{ userId: null }` with status `401` when no session exists. | Checks the current-session route without reading cookies directly and with the session seam mocked. |
| 16.5 | RED: `src/app/api/spots/route.test.ts` imports `POST`, `GET`, `PATCH`, and `DELETE` from `src/app/api/spots/route.ts`, but the saved-spot route handlers did not exist yet, so the test fails for the expected missing route implementation. | GREEN: `POST`, `GET`, `PATCH`, and `DELETE` were added in `src/app/api/spots/route.ts`; they create, list, update, and delete saved spots via Prisma, scoped by `x-user-id`, while passing full-precision coordinates through unchanged. | Checks that saved-spot API routes preserve coordinate precision and keep every operation scoped to the logged-in user. |
| 16.6 | RED: `src/app/api/search/route.test.ts` imports `POST` from `src/app/api/search/route.ts`, but the search route handler did not exist yet, so the test fails for the expected missing route implementation. | GREEN: `POST` was added in `src/app/api/search/route.ts`; it reads species, full-precision latitude/longitude, and month, fetches occurrence records with mocked `fetch`, excludes `0,0` records before mapping and rate calculation, calls `computeSightingRate`, calls the mocked AI explanation seam, and returns `{ rate, occurrences, explanation }`. | Checks that the search route ties occurrence fetch, Null Island filtering, sighting-rate sample size/confidence, and AI explanation wiring together without inventing data. |
| Species list | RED: `src/lib/species.test.ts` imports `getSpeciesForWaterType` from `@/lib/species`, but the implementation module did not exist yet, so the test fails for the expected missing species-list lookup. | GREEN: `getSpeciesForWaterType` was added in `src/lib/species.ts`; it returns separate non-empty saltwater and freshwater lists with common and scientific names, including Striped Bass as `Morone saxatilis` for saltwater and Brook Trout as `Salvelinus fontinalis` for freshwater, while excluding Black Drum from saltwater. | Checks that selectable species are shared, water-type specific, and mapped to real scientific names. |
| Common species name | RED: `src/lib/species.test.ts` imports `getCommonName` from `@/lib/species`, but the implementation did not export it yet, so the test fails for the expected missing common-name lookup. | GREEN: `getCommonName` was added in `src/lib/species.ts`; it searches both the saltwater and freshwater species lists by scientific name, returns the matching `commonName`, and returns the original scientific name unchanged when no match exists. | Checks that `Morone saxatilis` maps to `Striped Bass`, `Salvelinus fontinalis` maps to `Brook Trout`, and unknown scientific names are preserved unchanged. |
| Sighting-rate explanation | RED: `src/lib/sightingRateExplanation.test.ts` mocks OpenAI's chat completions response and expects `explainSightingRate` to call `https://api.openai.com/v1/chat/completions`, include `Striped Bass`, `June`, `matchingMonthCount`, and `totalCount` in the request body, and return `choices[0].message.content`; the implementation was only a local string formatter. | GREEN: `explainSightingRate` now calls OpenAI chat completions with `gpt-4o-mini`, uses `getCommonName`, converts the month number to a month name, sends only the computed factual values in system/user messages, and returns the mocked OpenAI content string. | Keeps AI as explanation/assembly only: the rate, sample size, confidence, coordinates, species, and month are provided by code. |
| Verification email send | RED: `src/lib/email.test.ts` imports `sendVerificationEmail` and expects it to POST to Resend with the API key, from/to fields, and verification code in the body, but `src/lib/email.ts` was still a stub returning `verification-email-not-configured`. | GREEN: `sendVerificationEmail` now POSTs to `https://api.resend.com/emails` with `Authorization: Bearer ${process.env.RESEND_API_KEY}`, `Content-Type: application/json`, `from: onboarding@resend.dev`, the requested recipient, a verification subject, HTML containing the code, and returns the Resend response id. | Checks the email seam with `fetch` mocked, so the unit test never sends a real email. |
| Best month | RED: `src/lib/bestMonth.test.ts` imports `findBestMonth` from `@/lib/bestMonth`, but the implementation module did not exist yet, so the test fails for the expected missing best-month calculation. | GREEN: `findBestMonth` was added in `src/lib/bestMonth.ts`; it parses UTC months from occurrence `eventDate` strings, counts records by month, returns the highest-count month, computes that month's rate as count divided by total records, and reports the tested confidence tier. | Checks that the best historical month is computed from occurrence dates with sample size and confidence metadata. |
| Home highlights | RED: `src/lib/homeHighlights.test.ts` imports `getHomeHighlights` from `@/lib/homeHighlights`, but the implementation module does not exist yet, so the test fails for the expected missing home-highlight card assembly. | GREEN: Not recorded yet; implementation for Home Highlights has not been written. | Checks that home highlight cards come from mocked GBIF-shaped occurrence records and report the dominant month, rate, sample size, confidence, coordinates, and species labels without hitting the network in unit tests. |
